<resources>
    <!-- Thème de fenêtre (splash). Le rendu réel est piloté par Compose. -->
    <style name="Theme.RegistreBoucles" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/fond</item>
        <item name="android:statusBarColor">@color/marine_fonce</item>
        <item name="android:navigationBarColor">@color/fond</item>
    </style>
</resources>
